server {
    listen 80;
    server_name localhost current.seh-nya.com;

    # Favicon
    location /favicon.ico {
        proxy_pass http://web:3000/favicon.ico;
        proxy_set_header Host $host;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Frontend (Next.js)
    location / {
        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # API (FastAPI)
    location /api/ {
        proxy_pass http://api:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# SSL redirect (add after getting SSL certificate)
# server {
#     listen 80;
#     server_name current.seh-nya.com;
#     return 301 https://$server_name$request_uri;
# }

# server {
#     listen 443 ssl;
#     server_name current.seh-nya.com;
#     
#     ssl_certificate /etc/letsencrypt/live/current.seh-nya.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/current.seh-nya.com/privkey.pem;
#     
#     # Same location blocks as above
# }